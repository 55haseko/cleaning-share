# ===== バックエンド本番環境設定 (.env) =====
# VPSデプロイ時にこのファイルを .env にコピーして使用してください
# cp .env.production.example .env

# ===== データベース設定 =====
DB_HOST=localhost
DB_USER=cleaning_user
DB_PASSWORD=your-strong-password-here-change-me
DB_NAME=cleaning_system

# ===== サーバー設定 =====
PORT=4001
NODE_ENV=production

# ===== JWT認証設定 =====
# 以下のコマンドで生成してください:
# node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-very-long-random-secret-key-change-this-in-production-min-64-chars

# ===== CORS設定 =====
# 本番ドメインを指定（IPアドレスでも可）
CORS_ORIGIN=http://your-domain.com
# 複数ドメインの場合はカンマ区切り
# CORS_ORIGIN=http://your-domain.com,https://your-domain.com

# ===== ストレージ設定 =====
STORAGE_ROOT=/var/www/cleaning-share/backend/uploads
# 本番環境でのベースURL（Nginxで配信するURL）
PUBLIC_BASE_URL=http://your-domain.com

# ===== ファイル保持ポリシー =====
# 写真の保持期間（日数）
RETENTION_DAYS_PHOTO=60
# バンドル（ZIP/PDF）の保持期間（日数）
RETENTION_DAYS_BUNDLE=365

# ===== アップロード制限 =====
# 最大ファイルサイズ（MB）
MAX_FILE_SIZE_MB=20
# 一度にアップロード可能な最大ファイル数
MAX_FILES_PER_UPLOAD=20

# ===== ログ設定 =====
LOG_LEVEL=info
# ログファイルの保存先
LOG_FILE_PATH=/var/www/cleaning-share/backend/logs

# ===== Cron設定 =====
# 古いファイル削除のスケジュール（cron形式）
# デフォルト: 毎日 AM 2:00
CLEANUP_CRON_SCHEDULE=0 2 * * *

# ===== セキュリティ設定 =====
# bcryptのラウンド数（推奨: 10-12）
BCRYPT_ROUNDS=10

# ===== レート制限 =====
# 15分間の最大リクエスト数
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# ===== メール設定（オプション：将来の機能用） =====
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# SMTP_FROM=noreply@your-domain.com

# ===== その他 =====
# タイムゾーン
TZ=Asia/Tokyo
