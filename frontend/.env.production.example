# ===== フロントエンド本番環境設定 (.env) =====
# VPSデプロイ時にこのファイルを .env.production にコピーして使用してください
# cp .env.production.example .env.production

# ===== API設定 =====
# バックエンドAPIのベースURL
# 本番ドメインまたはIPアドレスを指定してください
REACT_APP_API_URL=http://your-domain.com/api

# ===== ビルド設定 =====
# 本番ビルド時の最適化
GENERATE_SOURCEMAP=false

# ===== その他の設定 =====
# タイムゾーン（バックエンドと合わせる）
TZ=Asia/Tokyo

# ===== 注意事項 =====
# 1. REACT_APP_ プレフィックスが必須です（React環境変数の規約）
# 2. .env.production は本番サーバーでビルド時に使用されます
# 3. ビルド後の静的ファイルには環境変数は含まれません（ビルド時に埋め込まれます）
# 4. 変更後は必ず再ビルドが必要です: npm run build
# 5. セキュリティ: フロントエンドの環境変数はブラウザから見えるため、秘密情報は含めないでください

# ===== Nginx設定例 =====
# 本番環境では以下のようなNginx設定を推奨:
#
# server {
#     listen 80;
#     server_name your-domain.com;
#
#     # フロントエンド（Reactビルド済み静的ファイル）
#     root /var/www/cleaning-share/frontend/build;
#     index index.html;
#
#     location / {
#         try_files $uri $uri/ /index.html;
#     }
#
#     # バックエンドAPI
#     location /api {
#         proxy_pass http://localhost:4001;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_cache_bypass $http_upgrade;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     }
#
#     # アップロードされた静的ファイル（写真・領収書）
#     location /uploads {
#         alias /var/www/cleaning-share/backend/uploads;
#         expires 30d;
#         add_header Cache-Control "public, immutable";
#     }
# }
